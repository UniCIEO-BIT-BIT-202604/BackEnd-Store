info:
  name: Re New Token
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/auth/renew-token
  headers:
    - name: X-Token
      value: aqui-va-el-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
