info:
  name: Get Cart
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/cart
  headers:
    - name: X-Token
      value: {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
