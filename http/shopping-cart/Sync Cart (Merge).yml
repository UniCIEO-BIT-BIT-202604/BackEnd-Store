info:
  name: Sync Cart (Merge)
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/cart/sync
  headers:
    - name: X-Token
      value: {{token}}
  body:
    type: json
    data: |-
      {
        "items": [
          {
            "product": "60d5ecb8b5c9c22b8c8b4567",
            "quantity": 1
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
